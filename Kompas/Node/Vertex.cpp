/*
 * #include "Vertex.h"
 * 
 * Vertex::operator const Vertex::Point3D() {
 * 	K5::ksVertexDefinitionPtr def = pDefinition;
 * 	double x, y, z;
 * 	if (def->GetPoint(&x, &y, &z)) {
 * 		return Vertex::Point3D{x, y, z};
 * 	} else {
 * 		return Vertex::Point3D{0, 0, 0};
 * 	}
 * }
 * 
 * int Vertex::TYPE = KConst3D::o3d_vertex;
 */
